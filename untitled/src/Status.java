/**
 * Represents the state of each playground whether it is active (activate), or
 * suspended (not available on the system), or not activate (bot active now).
 * @author Mariam Khaled
 * @version 1.0
 * @since June 2021
 *
 */
public enum Status {
    Activate,
    Not_Activate,
    Suspend;
}